<?php
/**
 * {{ objectName }} create/edit form (Tailwind theme) — auto-generated CRUD view.
 *
 * Variables:
 *   $this->model — the {{ objectName }} model instance (empty props when creating)
 *   $this->error — optional error flash message
 */
defined('SP') or die('No startpoint defined...');
$isNew = ($this->model->{{ primaryKey }} ?? 0) <= 0;
?>
<div class="max-w-2xl mx-auto py-6 px-4">
    <?php echo \Pramnos\Application\Application::getInstance()->renderBreadcrumbs(); ?>
    <h2 class="mb-6"><?php echo $isNew ? 'New {{ objectName }}' : 'Edit {{ objectName }}'; ?></h2>
    <?php if (!empty($this->error)): ?>
        <div class="bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-sm mb-4"><?php echo htmlspecialchars($this->error); ?></div>
    <?php endif; ?>
    <div class="bg-white rounded-xl shadow-xs border border-gray-200">
        <div class="p-5">
            <form method="post" action="<?php echo sURL; ?>{{ className }}/save/<?php echo $this->model->{{ primaryKey }}; ?>">
                <?php echo \Pramnos\Http\Middleware\CsrfMiddleware::tokenField(); ?>
{{ formFields }}
                <div class="flex gap-2 mt-4">
                    <button type="submit" class="px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-sm hover:bg-blue-700">Save</button>
                    <a href="<?php echo sURL; ?>{{ className }}" class="px-4 py-2 border border-gray-300 text-gray-700 text-sm rounded-sm hover:bg-gray-50">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>
