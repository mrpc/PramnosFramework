/**
 * Screens generated by `create:crud`.
 *
 * The list is maintained by the generator: each new CRUD adds its entry here,
 * and the application renders a navigation item for it. Reorder or relabel
 * freely — only the entries themselves are rewritten, never the whole file.
 */

export const screens = [
];
