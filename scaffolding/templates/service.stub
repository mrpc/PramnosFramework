<?php

namespace {{ namespace }};

use Pramnos\Application\Service;

/**
 * {{ class }} — application service.
 *
 * A service encapsulates one slice of application logic together with its data
 * access. In the services-oriented application style (see the Application Styles
 * guide), controllers stay thin and delegate to services like this instead of
 * putting behaviour on ActiveRecord models — the queries live here, behind
 * intention-revealing methods, and the framework QueryBuilder does the SQL.
 *
 * Extending Pramnos\Application\Service gives you three things and asks for
 * nothing: an injectable database resolved on first use (so a unit test that
 * never queries never connects), $this->queryBuilder() as the shortest path to
 * that connection, and $this->measure() to make a call show up in the debug
 * toolbar's Domain tab. The service appears there as soon as it runs; a call
 * appears with a duration once it is wrapped in measure().
 */
class {{ class }} extends Service
{
    // Add your service methods here, e.g.:
    //
    // public function findActive(): array
    // {
    //     return $this->measure('findActive', fn() => $this->queryBuilder('{{ table }}')
    //         ->where('active', '=', true)
    //         ->getAll());
    // }
}
