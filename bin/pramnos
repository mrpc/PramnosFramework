#!/usr/bin/env php
<?php declare(strict_types=1);

$possibleAutoloadPaths = [
     // dependency
     __DIR__ . '/../../../autoload.php',
     // local dev repository
     __DIR__ . '/../vendor/autoload.php',
];

foreach ($possibleAutoloadPaths as $possibleAutoloadPath) {
    if (file_exists($possibleAutoloadPath)) {
        require_once $possibleAutoloadPath;
        break;
    }
}

if (!defined('ROOT')) {
    // Check if we are running from a vendor directory (installed as dependency)
    if (strpos(__FILE__, DIRECTORY_SEPARATOR . 'vendor' . DIRECTORY_SEPARATOR) !== false) {
        // If we are in vendor/mrpc/pramnosframework/bin/pramnos
        define('ROOT', dirname(__DIR__, 4));
    } else {
        // We are in the framework repository itself (local dev)
        define('ROOT', dirname(__DIR__));
    }
}



$app = new \Pramnos\Console\Application();
$app->run();
