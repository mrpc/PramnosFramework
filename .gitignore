/vendor
/build/coverage/
/build/logs/
composer.phar
composer.lock
.php_cs.cache
.DS_Store
Thumbs.db
/nbproject/**/*
/nbproject
/.idea
/.idea/*.*
/_cache/*.*
/_cache/**/*
/_cache/*
*.log
.Spotlight-V100
.Trashes
Icon
desktop.ini
/.vagrant
.phpunit.result.cache
/coverage
/var/*
/coverage.xml
site/*
# Brand assets synced into the docs site at build time (source of truth: brand/)
/docs/assets/logo.png
/docs/assets/favicon.png

# Runtime app directory of a project built on the framework. In this repo it is
# only ever created as a side effect of the test suite (OAuth2 key generation,
# the model registry) — the tests clean up after themselves, this is the belt.
/app

# Test debris: the media tests write into ROOT/www/uploads
/www/

# Node tooling for the shipped JavaScript assets (see ./lintjs)
node_modules/
