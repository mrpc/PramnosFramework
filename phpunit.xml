<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         backupGlobals="false"
         bootstrap="tests/bootstrap.php"
         colors="true"
         processIsolation="false"
         stopOnError="false"
         stopOnFailure="false"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.0/phpunit.xsd"
         displayDetailsOnTestsThatTriggerDeprecations="true"
         displayDetailsOnTestsThatTriggerErrors="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
>
    <extensions>
        <!--
            Request-scoped state is process-wide in a test run. Without this,
            an identity sealed by one test answers for every test after it —
            which produced 135 failures in tests that had nothing to do with
            authentication.

            Both extensions live in src/, not tests/, because /tests is
            export-ignored: a scaffolded application needs them just as much
            and can only reference what the package actually ships.
        -->
        <bootstrap class="Pramnos\Framework\Testing\RequestIdentityIsolation"/>
        <!--
            The document is a mutable per-type singleton, and both code and tests
            write to it. Three failures in one session appeared only in a full run,
            because an earlier test had left the shared HTML document calling itself
            `raw` — and the fix each time looked like a bug in the test that failed.
        -->
        <bootstrap class="Pramnos\Framework\Testing\DocumentIsolation"/>
        <!--
            The gate registry is static: abilities, policies and before/after hooks
            registered by one test would answer for every test after it. Written with
            the feature rather than after the failures, which is what the other two
            paid for.
        -->
        <bootstrap class="Pramnos\Framework\Testing\GateIsolation"/>
    </extensions>

    <testsuites>
        <testsuite name="Unit Tests">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature Tests">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
        <testsuite name="Integration Tests">
            <directory suffix="Test.php">./tests/Integration</directory>
        </testsuite>
        <testsuite name="Characterization Tests">
            <directory suffix="Test.php">./tests/Characterization</directory>
        </testsuite>
        <!--
            The only suite `dockertest` does not run by default.

            These drive a whole subsystem against the real thing rather than against a
            fixture: the OAuth2 client against the framework's own authorization server,
            for instance. That means rebuilding schema objects from their canonical
            migrations, and the suite shares one database, so such a test is not a peer of
            the sixteen thousand around it. It has to own the tables it rebuilds, and
            everything else has to be elsewhere while it does.

            Keeping them out of the default run also keeps their cost out of it: RSA key
            generation and repeated migrations are not worth paying for on every run.
            Run them deliberately, with the e2e flag described in the Testing Guide.

            The name has no spaces because dockertest passes it to PHPUnit through an
            unquoted variable, where a space would split it into two arguments and select
            nothing.
        -->
        <testsuite name="EndToEnd">
            <directory suffix="Test.php">./tests/EndToEnd</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">./src</directory>
        </include>
    </source>
    <coverage>
        <report>
            <clover outputFile="coverage/clover.xml"/>
        </report>
    </coverage>
</phpunit>
